//every {it work as a logical AND operator in which every condition must be true}
[2,4,6].every((element)=>(element%2==0)) 

//some {it work as a logical OR operator in which even one condtion is true all condition will be true}
// [2,5,6].some((element)=>(element%2 == 0))