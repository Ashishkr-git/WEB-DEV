const sum = (a, b) => {
  console.log(a + b);
};

const cube = (n) => {
  console.log(Math.pow(n, 3));
};

const multiple = (a) => {
  console.log(Math.pow(a, a));
};

//implicit return

const func = (a,b) => (a*b);
